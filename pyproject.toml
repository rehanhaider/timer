[project]
name = "timer"
version = "0.1.1"
description = "A CLI based timer using Textual"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "textual>=6.11.0",
    "textual-dev>=1.8.0",
    "typer>=0.21.0",
]

[project.scripts]
timer = "app:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/app.py", "src/tui"]
